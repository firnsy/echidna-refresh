Echidna RESTful API:

Dependencies:
- Mojolicious
- AnyEvent
- EV
- Morbo Server (Mojolicious Non-blocking server)

* AnyEvent
  - EV
  - AnyEvent::DBI




# TESTING

# move to the root of the source

# start echidnad
PERL5LIB=./lib morbo --listen='http://localhost:6970' ./echidnad

# start cxtrackerd
PERL5LIB=./lib morbo --listen 'http://localhost:6969' ./cxtrackerd

# start barnyard2d
PERL5LIB=./lib morbo --listen 'http://localhost:6968' ./barnyard2d

# start passivednsd
PERL5LIB=./lib morbo --listen 'http://localhost:6967' ./passivednsd
