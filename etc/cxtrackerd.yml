#
# NETWORK
#
# Specify the network settings for the server
#
host: localhost
port: 6969
key: 'woot'

host: localhost
port: 6967
key: 'woot'

mode: push

server:
  host: localhost
  port: 6970

#
# CXTRACKER Configuration
#

cxtracker:
  dir:      './logs/cxtracker'
  mode:     'archive'
  archive:  './logs/cxtracker/archive'
  regex:    'stats.*'
  poll:     10
